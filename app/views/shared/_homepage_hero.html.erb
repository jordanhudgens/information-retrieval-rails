<div class="jumbotron">
	<h2><b>
		Stack Overflow Search
	</b></h2>
	<p> 
		Search for what you want from Stack Overflow
	</p>
	<p>
		<a id="modal-287366" href="#modal-container-287366" role="button" class="btn btn-primary btn-large" data-toggle="modal">Read More</a>
	</p>
	<p>
		<% if current_user %>
			<%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: 'btn btn-default' %>
		<% else %>
			<%= link_to "Register", new_user_registration_path, class: 'btn btn-warning' %>
			<%= link_to "Sign In", new_user_session_path, class: 'btn btn-danger' %>
		<% end %>
	</p>
</div>